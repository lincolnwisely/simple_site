Supersimple project for quick one-off pages and demos. E.g., a localized CodePen sandbox boilerplate buzzword blah.

To compile sass: 

`npm install` to load node packages
`npm start` to run lint and compile css